using System.Collections;
using System.Collections.Generic;
using UnityEngine;

static public class Units {
    public enum Player {
        Player = 0,
    }

    public enum Enemigos{
        Zombie = 0,
    }

    public enum Armas {
        Pistola = 0,
    }

    static public float playerSpeed = 35;

    static public float zombieSpeed = 30;
    static public int zombiePoints = 5;
	
}
